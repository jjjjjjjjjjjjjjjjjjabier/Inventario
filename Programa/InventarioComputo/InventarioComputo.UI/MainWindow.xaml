<Window x:Class="InventarioComputo.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:views="clr-namespace:InventarioComputo.UI.Views"
        xmlns:vm="clr-namespace:InventarioComputo.UI.ViewModels"
        mc:Ignorable="d"
        Title="Inventario de Cómputo - Henniges Automotive" Height="720" Width="1280"
        WindowStartupLocation="CenterScreen"
        Background="#F0F2F5">

    <Window.Resources>
        <DataTemplate DataType="{x:Type vm:EstadosViewModel}">
            <views:EstadosView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:UnidadesViewModel}">
            <views:UnidadesView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:UbicacionesViewModel}">
            <views:UbicacionesView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:TiposEquipoViewModel}">
            <views:TiposEquipoView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:EquiposComputoViewModel}">
            <views:EquiposComputoView />
        </DataTemplate>

        <Style x:Key="NavButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#CDD5E0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Padding" Value="20,12"/>
            <Setter Property="Margin" Value="0,4"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#4A6A96"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="240"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="#2C4A73" Padding="10">
            <StackPanel>
                <TextBlock Text="Inventario" Foreground="White" FontSize="28" FontWeight="Bold" Margin="10,10,10,30" HorizontalAlignment="Center"/>

                <TextBlock Text="INVENTARIO" Foreground="#A0B0C4" FontSize="12" FontWeight="SemiBold" Margin="15,0,0,5"/>
                <Button Content="Equipos de Cómputo" Style="{StaticResource NavButtonStyle}" Command="{Binding NavigateToEquiposComputoCommand}"/>
                <Button Content="Asignaciones" Style="{StaticResource NavButtonStyle}" IsEnabled="False"/>

                <Separator Margin="15,20" Background="#4A6A96"/>

                <TextBlock Text="CATÁLOGOS" Foreground="#A0B0C4" FontSize="12" FontWeight="SemiBold" Margin="15,0,0,5"/>
                <Button Content="Estados" Style="{StaticResource NavButtonStyle}" Command="{Binding NavigateToEstadosCommand}"/>
                <Button Content="Unidades" Style="{StaticResource NavButtonStyle}" Command="{Binding NavigateToUnidadesCommand}"/>
                <Button Content="Ubicaciones" Style="{StaticResource NavButtonStyle}" Command="{Binding NavigateToUbicacionesCommand}"/>
                <Button Content="Tipos de Equipo" Style="{StaticResource NavButtonStyle}" Command="{Binding NavigateToTiposEquipoCommand}"/>
            </StackPanel>
        </Border>

        <ContentControl Grid.Column="1"
                        Content="{Binding CurrentViewModel}"
                        Margin="10"/>
    </Grid>
</Window>