<Window x:Class="InventarioComputo.UI.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:beh="clr-namespace:InventarioComputo.UI.Behaviors"
        Title="Iniciar Sesión"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Width="520" Height="440"
        Background="#0F172A"
        FontFamily="Segoe UI">
    <Grid>
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                <GradientStop Color="#0F172A" Offset="0"/>
                <GradientStop Color="#111827" Offset="1"/>
            </LinearGradientBrush>
        </Grid.Background>

        <Border Width="420" Height="340"
                CornerRadius="16"
                Background="#FFFFFF"
                Padding="28"
                HorizontalAlignment="Center" VerticalAlignment="Center">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0"
                           Text="Inventario de Equipos de Cómputo"
                           FontSize="20" FontWeight="Semibold" Foreground="#1F2937"
                           Margin="0,0,0,10"/>

                <StackPanel Grid.Row="1" Margin="0,0,0,0">
                    <TextBlock Text="Usuario" Foreground="#6B7280" FontSize="12" Margin="0,0,0,2"/>
                    <TextBox Text="{Binding Usuario, UpdateSourceTrigger=PropertyChanged}"
                             Height="32" Padding="8,2"
                             FontSize="13"
                             Background="#F9FAFB" BorderBrush="#E5E7EB" BorderThickness="1"
                             Foreground="#111827"/>
                </StackPanel>

                <StackPanel Grid.Row="2" Margin="0,6,0,0">
                    <TextBlock Text="Contraseña" Foreground="#6B7280" FontSize="12" Margin="0,0,0,2"/>
                    <PasswordBox Height="32" Padding="8,2"
                                 FontSize="13"
                                 Background="#F9FAFB" BorderBrush="#E5E7EB" BorderThickness="1"
                                 Foreground="#111827"
                                 beh:PasswordBoxBehavior.Attach="True"
                                 beh:PasswordBoxBehavior.Password="{Binding Password, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                </StackPanel>

                <Grid Grid.Row="3"/>

                <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Iniciar Sesión"
                            Command="{Binding LoginCommand}"
                            IsDefault="True"
                            Margin="0,0,10,0"
                            Padding="16,8"
                            Background="#2563EB"
                            Foreground="White"
                            BorderBrush="#1D4ED8"
                            BorderThickness="1"
                            Cursor="Hand">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border CornerRadius="8"
                                        Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="4"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                        <Button.Style>
                            <Style TargetType="Button">
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#1D4ED8"/>
                                    </Trigger>
                                    <Trigger Property="IsEnabled" Value="False">
                                        <Setter Property="Background" Value="#93C5FD"/>
                                        <Setter Property="BorderBrush" Value="#93C5FD"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>

                    <Button Content="Cancelar"
                            Command="{Binding CancelarCommand}"
                            IsCancel="True"
                            Padding="16,8"
                            Background="#F3F4F6"
                            Foreground="#111827"
                            BorderBrush="#D1D5DB"
                            BorderThickness="1"
                            Cursor="Hand">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border CornerRadius="8"
                                        Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="4"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>