<Window x:Class="InventarioComputo.UI.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:behaviors="clr-namespace:InventarioComputo.UI.Behaviors"
        Title="Iniciar sesión" Height="360" Width="420"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="12"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="12"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Inicio de sesión" FontSize="20" FontWeight="SemiBold"/>

        <StackPanel Grid.Row="1" Margin="0,16,0,0">
            <Label Content="Usuario" Style="{StaticResource LabelStyle}"/>
            <TextBox Text="{Binding Usuario, UpdateSourceTrigger=PropertyChanged}"
                     Style="{StaticResource TextBoxStyle}" MinWidth="240"/>

            <Label Content="Contraseña" Margin="0,10,0,0" Style="{StaticResource LabelStyle}"/>
            <PasswordBox behaviors:PasswordBoxBehavior.Attach="True"
                         behaviors:PasswordBoxBehavior.Password="{Binding Password, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                         MinWidth="240" Height="30" Padding="6"/>
        </StackPanel>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Ingresar" Command="{Binding LoginCommand}" IsDefault="True"
                    Style="{StaticResource DialogButtonStyle}" Margin="0,0,8,0"/>
            <Button Content="Cancelar" Command="{Binding CancelarCommand}" IsCancel="True"
                    Style="{StaticResource ActionButtonStyle}" Background="#888"/>
        </StackPanel>
    </Grid>
</Window>