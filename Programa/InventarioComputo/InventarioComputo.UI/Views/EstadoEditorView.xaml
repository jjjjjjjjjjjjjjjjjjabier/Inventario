<Window x:Class="InventarioComputo.UI.Views.EstadoEditorView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding Titulo}"
        Width="400" SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Campos -->
        <Label Grid.Row="0" Content="Nombre:"/>
        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Nombre, UpdateSourceTrigger=PropertyChanged}" Margin="0,5"/>

        <Label Grid.Row="1" Content="Descripción:"/>
        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Descripcion}" Margin="0,5"/>

        <Label Grid.Row="2" Content="Color:"/>
        <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding ColorHex}" Margin="0,5"/>

        <CheckBox Grid.Row="3" Grid.Column="1" Content="Activo" IsChecked="{Binding Activo}" Margin="0,10"/>

        <!-- Botones -->
        <StackPanel Grid.Row="4" Grid.ColumnSpan="2" Orientation="Horizontal" 
                    HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="Guardar" Command="{Binding GuardarCommand}" />
            <Button Content="Cancelar" Command="{Binding CloseCommand}" />
        </StackPanel>
    </Grid>
</Window>