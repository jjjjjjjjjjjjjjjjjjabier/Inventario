<Window x:Class="InventarioComputo.UI.Views.EstadoEditorView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Editor de Estado" 
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        Width="400" 
        ResizeMode="NoResize">
    <StackPanel Margin="16">
        <TextBlock Text="Nombre" Style="{StaticResource LabelStyle}"/>
        <TextBox Text="{Binding Nombre, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8"/>

        <TextBlock Text="Descripción" Style="{StaticResource LabelStyle}"/>
        <TextBox Text="{Binding Descripcion, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8"/>

        <TextBlock Text="Color (Hex #RRGGBB)" Style="{StaticResource LabelStyle}"/>
        <TextBox Text="{Binding ColorHex, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8"/>

        <CheckBox Content="Activo" IsChecked="{Binding Activo}" Margin="0,8,0,16"/>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">

            <Button Content="Guardar" 
                    Command="{Binding GuardarCommand}" 
                    CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"
                    IsDefault="True" MinWidth="80"/>

            <Button Content="Cancelar" IsCancel="True" Background="Gray" MinWidth="80" Margin="8,0,0,0"/>
        </StackPanel>
    </StackPanel>
</Window>