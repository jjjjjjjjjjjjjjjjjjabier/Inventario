<UserControl x:Class="InventarioComputo.UI.Views.TrabajadoresView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:InventarioComputo.UI.Views"
             mc:Ignorable="d" 
             Loaded="UserControl_Loaded">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Trabajadores" 
                   FontSize="24" FontWeight="Medium" 
                   Margin="0,10,0,20"/>

        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="300"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <CheckBox Grid.Column="0" Content="Mostrar inactivos" 
                      IsChecked="{Binding MostrarInactivos}" 
                      VerticalAlignment="Center" Margin="0,0,10,0"/>
            <TextBox Grid.Column="1" Text="{Binding FiltroTexto, UpdateSourceTrigger=PropertyChanged}"
                     Padding="5" Margin="0,0,10,0" 
                     VerticalAlignment="Center"
                     PlaceholderText="Buscar trabajadores..."
                     BorderThickness="1"/>
            <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Nuevo" 
                        Command="{Binding CrearCommand}" 
                        Style="{StaticResource ActionButtonStyle}" 
                        Background="#007BFF" 
                        Foreground="White" />
                <Button Content="Editar" 
                        Command="{Binding EditarCommand}" 
                        Style="{StaticResource ActionButtonStyle}" />
                <Button Content="Desactivar" 
                        Command="{Binding EliminarCommand}" 
                        Style="{StaticResource ActionButtonStyle}" />
            </StackPanel>
        </Grid>

        <DataGrid Grid.Row="2" 
                  ItemsSource="{Binding Trabajadores}" 
                  SelectedItem="{Binding TrabajadorSeleccionado}"
                  AutoGenerateColumns="False" 
                  IsReadOnly="True"
                  GridLinesVisibility="Horizontal"
                  BorderThickness="1"
                  HeadersVisibility="Column"
                  CanUserAddRows="False"
                  SelectionMode="Single"
                  SelectionUnit="FullRow">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Número Empleado" Binding="{Binding NumeroEmpleado}" Width="120"/>
                <DataGridTextColumn Header="Nombre Completo" Binding="{Binding NombreCompleto}" Width="200"/>
                <DataGridTextColumn Header="Departamento" Binding="{Binding Departamento}" Width="150"/>
                <DataGridTextColumn Header="Puesto" Binding="{Binding Puesto}" Width="150"/>
                <DataGridCheckBoxColumn Header="Activo" Binding="{Binding Activo}" Width="60"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>