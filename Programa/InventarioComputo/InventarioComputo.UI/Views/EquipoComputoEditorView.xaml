<Window x:Class="InventarioComputo.UI.Views.EquipoComputoEditorView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        mc:Ignorable="d"
        Title="Editor de Equipo de Cómputo" Height="600" Width="800"
        WindowStartupLocation="CenterOwner">

    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <i:InvokeCommandAction Command="{Binding LoadedCommand}" />
        </i:EventTrigger>
    </i:Interaction.Triggers>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="16">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBlock Text="* No. de Serie" Style="{StaticResource LabelStyle}"/>
                        <TextBox Text="{Binding NumeroSerie}" Margin="0,0,0,8"/>

                        <TextBlock Text="Etiqueta de Inventario" Style="{StaticResource LabelStyle}"/>
                        <TextBox Text="{Binding EtiquetaInventario}" Margin="0,0,0,8"/>

                        <TextBlock Text="* Marca" Style="{StaticResource LabelStyle}"/>
                        <TextBox Text="{Binding Marca}" Margin="0,0,0,8"/>

                        <TextBlock Text="* Modelo" Style="{StaticResource LabelStyle}"/>
                        <TextBox Text="{Binding Modelo}" Margin="0,0,0,8"/>

                        <TextBlock Text="* Tipo de Equipo" Style="{StaticResource LabelStyle}"/>
                        <ComboBox ItemsSource="{Binding TiposEquipo}" SelectedItem="{Binding TipoEquipoSeleccionado}" DisplayMemberPath="Nombre" Margin="0,0,0,8"/>

                        <TextBlock Text="* Estado" Style="{StaticResource LabelStyle}"/>
                        <ComboBox ItemsSource="{Binding Estados}" SelectedItem="{Binding EstadoSeleccionado}" DisplayMemberPath="Nombre" Margin="0,0,0,8"/>

                        <TextBlock Text="Fecha de Adquisición" Style="{StaticResource LabelStyle}"/>
                        <DatePicker SelectedDate="{Binding FechaAdquisicion}" Margin="0,0,0,8"/>
                    </StackPanel>

                    <StackPanel Grid.Column="2">
                        <TextBlock Text="* Ubicación" FontWeight="SemiBold" Margin="0,0,0,4"/>
                        <TextBlock Text="Sede" Style="{StaticResource LabelStyle}"/>
                        <ComboBox ItemsSource="{Binding Sedes}" SelectedItem="{Binding SedeSeleccionada}" DisplayMemberPath="Nombre" Margin="0,0,0,8"/>

                        <TextBlock Text="Área" Style="{StaticResource LabelStyle}"/>
                        <ComboBox ItemsSource="{Binding Areas}" SelectedItem="{Binding AreaSeleccionada}" DisplayMemberPath="Nombre" Margin="0,0,0,8"/>

                        <TextBlock Text="* Zona" Style="{StaticResource LabelStyle}"/>
                        <ComboBox ItemsSource="{Binding Zonas}" SelectedItem="{Binding ZonaSeleccionada}" DisplayMemberPath="Nombre" Margin="0,0,0,24"/>

                        <TextBlock Text="Características" Style="{StaticResource LabelStyle}"/>
                        <TextBox Text="{Binding Caracteristicas}" Height="80" TextWrapping="Wrap" AcceptsReturn="True" Margin="0,0,0,8"/>

                        <TextBlock Text="Observaciones" Style="{StaticResource LabelStyle}"/>
                        <TextBox Text="{Binding Observaciones}" Height="80" TextWrapping="Wrap" AcceptsReturn="True" Margin="0,0,0,8"/>
                    </StackPanel>
                </Grid>
            </StackPanel>
        </ScrollViewer>

        <Border Grid.Row="1" BorderThickness="0,1,0,0" BorderBrush="#DDD" Padding="16" Background="#F8F8F8">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Guardar" Command="{Binding GuardarCommand}"
                        CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"
                        IsDefault="True" MinWidth="100" Margin="0,0,8,0"/>
                <Button Content="Cancelar" IsCancel="True" MinWidth="100"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>