<Window x:Class="InventarioComputo.UI.Views.EmpleadoEditorView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:sys="clr-namespace:System;assembly=System.Runtime"
        mc:Ignorable="d"
        Title="{Binding Titulo}"
        Height="250" Width="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Nombre Completo -->
        <TextBlock Grid.Row="0" Text="Nombre Completo:" VerticalAlignment="Center"/>
        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding NombreCompleto}" Margin="0,5"/>

        <!-- Puesto -->
        <TextBlock Grid.Row="1" Text="Puesto:" VerticalAlignment="Center"/>
        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Puesto}" Margin="0,5"/>

        <!-- Activo -->
        <TextBlock Grid.Row="2" Text="Estado:" VerticalAlignment="Center"/>
        <CheckBox Grid.Row="2" Grid.Column="1" Content="Activo" IsChecked="{Binding Activo}" Margin="0,5"/>

        <!-- Botones -->
        <StackPanel Grid.Row="4" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Guardar" Command="{Binding GuardarCommand}" Width="80" Margin="0,0,8,0"/>
            <Button Content="Cancelar" Command="{Binding CloseCommand}" Width="80"/>
        </StackPanel>

        <!-- Indicador de carga -->
        <Border Grid.Row="0" Grid.RowSpan="5" Grid.ColumnSpan="2" 
                Background="#80000000" 
                Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="Guardando..." Foreground="White" FontSize="16" HorizontalAlignment="Center"/>
                <ProgressBar IsIndeterminate="True" Height="4" Width="200" Margin="0,4,0,0"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>