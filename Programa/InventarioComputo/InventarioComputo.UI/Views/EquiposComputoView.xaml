<UserControl x:Class="InventarioComputo.UI.Views.EquiposComputoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000">

    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <i:InvokeCommandAction Command="{Binding LoadedAsyncCommand}" />
        </i:EventTrigger>
    </i:Interaction.Triggers>

    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,0,0,8">
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                <Button Content="Nuevo Equipo" Command="{Binding CrearAsyncCommand}"/>
                <Button Content="Editar Equipo" Command="{Binding EditarAsyncCommand}"/>
                <Button Content="Eliminar Equipo" Command="{Binding EliminarAsyncCommand}"/>
            </StackPanel>
            <Button Content="Refrescar" Command="{Binding BuscarAsyncCommand}" HorizontalAlignment="Right"/>
        </DockPanel>

        <DataGrid Grid.Row="1" ItemsSource="{Binding Equipos}"
                  SelectedItem="{Binding EquipoSeleccionado, Mode=TwoWay}"
                  AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="No. Serie" Binding="{Binding NumeroSerie}" Width="*"/>
                <DataGridTextColumn Header="Etiqueta" Binding="{Binding EtiquetaInventario}" Width="*"/>
                <DataGridTextColumn Header="Tipo" Binding="{Binding TipoEquipo.Nombre}" Width="*"/>
                <DataGridTextColumn Header="Marca" Binding="{Binding Marca}" Width="*"/>
                <DataGridTextColumn Header="Modelo" Binding="{Binding Modelo}" Width="*"/>
                <DataGridTextColumn Header="Estado" Binding="{Binding Estado.Nombre}" Width="*"/>
                <DataGridTextColumn Header="Ubicación" Binding="{Binding Zona.Nombre}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <Border Grid.Row="1" Background="#7F000000" CornerRadius="5"
                Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}">
            <TextBlock Text="Cargando..." Foreground="White" FontSize="18"
                       HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Border>
    </Grid>
</UserControl>