<UserControl x:Class="InventarioComputo.UI.Views.EquiposComputoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:InventarioComputo.UI.Views"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800"
             Loaded="UserControl_Loaded">

    <UserControl.Resources>
        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#0078D7"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="15,5"/>
            <Setter Property="MinWidth" Value="80"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}"
                                CornerRadius="20">
                            <ContentPresenter HorizontalAlignment="Center" 
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#0063B1"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#CCCCCC"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#EFEFEF"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="Padding" Value="15,5"/>
            <Setter Property="MinWidth" Value="80"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}"
                                CornerRadius="20">
                            <ContentPresenter HorizontalAlignment="Center" 
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#DDDDDD"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" 
                   Text="Equipos de Cómputo"
                   FontSize="24" 
                   FontWeight="Bold"
                   Margin="0,0,0,20"
                   Foreground="#2C3E50"/>

        <!-- Toolbar -->
        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                <CheckBox Content="Mostrar inactivos" 
                          IsChecked="{Binding MostrarInactivos}"
                          VerticalAlignment="Center"
                          Margin="0,0,20,0"/>

                <TextBox Width="250" 
                         Height="35"
                         Padding="10,0"
                         VerticalContentAlignment="Center"
                         Text="{Binding FiltroTexto, UpdateSourceTrigger=PropertyChanged}"
                         BorderBrush="#CCCCCC"
                         Margin="0,0,10,0">
                    <TextBox.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="4"/>
                        </Style>
                    </TextBox.Resources>
                    <TextBox.Style>
                        <Style TargetType="TextBox">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="TextBox">
                                        <Border Background="{TemplateBinding Background}"
                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                CornerRadius="4">
                                            <Grid>
                                                <TextBlock Text="Buscar equipos..." 
                                                           Foreground="#AAA"
                                                           VerticalAlignment="Center"
                                                           HorizontalAlignment="Left"
                                                           Margin="10,0,0,0"
                                                           Visibility="{Binding Text.IsEmpty, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                                                <ScrollViewer x:Name="PART_ContentHost" 
                                                              Margin="{TemplateBinding Padding}" 
                                                              VerticalAlignment="Center"/>
                                            </Grid>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </TextBox.Style>
                </TextBox>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Content="Nuevo" 
                        Style="{StaticResource ActionButtonStyle}"
                        Command="{Binding CrearCommand}"
                        Margin="5,0"/>

                <Button Content="Editar" 
                        Style="{StaticResource SecondaryButtonStyle}"
                        Command="{Binding EditarCommand}"
                        Margin="5,0"/>

                <Button Content="Desactivar" 
                        Style="{StaticResource SecondaryButtonStyle}"
                        Command="{Binding EliminarCommand}"
                        Margin="5,0"/>

                <Button Content="Ver Historial" 
                        Style="{StaticResource SecondaryButtonStyle}"
                        Command="{Binding VerHistorialCommand}"
                        Margin="5,0"/>
            </StackPanel>
        </Grid>

        <!-- DataGrid -->
        <DataGrid Grid.Row="2" 
                  ItemsSource="{Binding Equipos}"
                  SelectedItem="{Binding EquipoSeleccionado}"
                  Margin="0,10,0,0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="No. Serie" Binding="{Binding NumeroSerie}" Width="120"/>
                <DataGridTextColumn Header="Etiqueta" Binding="{Binding EtiquetaInventario}" Width="120"/>
                <DataGridTextColumn Header="Marca" Binding="{Binding Marca}" Width="120"/>
                <DataGridTextColumn Header="Modelo" Binding="{Binding Modelo}" Width="120"/>
                <DataGridTextColumn Header="Tipo" Binding="{Binding TipoEquipo.Nombre}" Width="120"/>
                <DataGridTextColumn Header="Estado" Binding="{Binding Estado.Nombre}" Width="120"/>
                <DataGridTextColumn Header="Ubicación" Binding="{Binding Zona.Nombre}" Width="120"/>
                <DataGridTextColumn Header="Fecha Adq." Binding="{Binding FechaAdquisicion, StringFormat=dd/MM/yyyy}" Width="120"/>
                <DataGridTextColumn Header="Asignado a" Binding="{Binding Usuario.NombreCompleto}" Width="150"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Loading indicator -->
        <Grid Grid.Row="0" Grid.RowSpan="4" Background="#80FFFFFF"
              Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Text="Cargando..." 
                           FontSize="16"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,10"/>
                <ProgressBar IsIndeterminate="True" 
                             Width="200" 
                             Height="10"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>