<Window x:Class="InventarioComputo.UI.Views.AreaEditorView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Editor de Área"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        Width="400"
        ResizeMode="NoResize">
    <StackPanel Margin="16">
        <TextBlock Text="Nombre del Área" Style="{StaticResource LabelStyle}"/>
        <TextBox Text="{Binding Nombre, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8"/>
        <CheckBox Content="Activo" IsChecked="{Binding Activo}" Margin="0,8,0,16"/>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Guardar"
                    Command="{Binding GuardarCommand}"
                    CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"
                    IsDefault="True" MinWidth="80"/>
            <Button Content="Cancelar" IsCancel="True" Background="Gray" MinWidth="80" Margin="8,0,0,0"/>
        </StackPanel>
    </StackPanel>
</Window>