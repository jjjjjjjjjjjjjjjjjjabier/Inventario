<UserControl x:Class="InventarioComputo.UI.Views.ReportesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:conv="clr-namespace:InventarioComputo.UI.Converters"
             mc:Ignorable="d"
             d:DesignHeight="650" d:DesignWidth="950"
             Loaded="UserControl_Loaded">

    <UserControl.Resources>
        <conv:ActiveStatusConverter x:Key="ActiveStatusConverter"/>
        <conv:BoolToStringConverter x:Key="BoolToStringConverter"/>
        <conv:ObjectToVisibilityConverter x:Key="ObjectToVisibilityConverter"/>
        <conv:NotNullConverter x:Key="NotNullConverter"/>
        <conv:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>

        <!-- Estilo para textos en celdas: alineación, trimming y colores -->
        <Style x:Key="CellTextStyle" TargetType="TextBlock">
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Foreground" Value="#2C3E50"/>
            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
            <Setter Property="TextWrapping" Value="NoWrap"/>
            <Setter Property="Margin" Value="4,0"/>
        </Style>

        <!-- Estilo para encabezados de columna -->
        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#F5F6F8"/>
            <Setter Property="Foreground" Value="#2C3E50"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="BorderBrush" Value="#E5E7EB"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
        </Style>

        <!-- Estilo de fila: altura, hover y selección -->
        <Style TargetType="DataGridRow">
            <Setter Property="Height" Value="44"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#F3F6FB"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#E8F1FF"/>
                    <Setter Property="Foreground" Value="#1F2D3D"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Estilo de celda: padding y enfoque limpio -->
        <Style TargetType="DataGridCell">
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Background" Value="Transparent"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Foreground" Value="#1F2D3D"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Estilo para controles de formularios -->
        <Style x:Key="FormLabelStyle" TargetType="TextBlock">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,0,0,5"/>
            <Setter Property="Foreground" Value="#2C3E50"/>
        </Style>

        <Style x:Key="FormComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="BorderBrush" Value="#D1D5DB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="IsEditable" Value="False"/>
            <Setter Property="Height" Value="32"/>
        </Style>

        <Style x:Key="FormDatePickerStyle" TargetType="DatePicker">
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="BorderBrush" Value="#D1D5DB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Height" Value="32"/>
        </Style>

        <!-- Estilo para botones de acción -->
        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#3498DB"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="6"/>
                </Style>
            </Style.Resources>
        </Style>

        <Style x:Key="SecondaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#F0F2F5"/>
            <Setter Property="Foreground" Value="#2C3E50"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#D1D5DB"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="6"/>
                </Style>
            </Style.Resources>
        </Style>

        <Style x:Key="ExportButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="6"/>
                </Style>
            </Style.Resources>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Encabezado -->
        <Border Grid.Row="0" Background="#F5F7FA" BorderBrush="#E1E5EB" BorderThickness="0,0,0,1" Padding="24,16">
            <TextBlock Text="Reportes y Consultas" FontSize="22" FontWeight="Medium" Foreground="#2C3E50"/>
        </Border>

        <!-- Filtros - Panel mejorado con mejor espaciado -->
        <Border Grid.Row="1" Background="White" BorderBrush="#E1E5EB" BorderThickness="0,0,0,1" Padding="24,20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Fila 1 -->
                <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,0,12,16">
                    <TextBlock Text="Tipo de Equipo" Style="{StaticResource FormLabelStyle}"/>
                    <ComboBox ItemsSource="{Binding TiposEquipo}"
                              SelectedItem="{Binding TipoEquipoSeleccionado}"
                              DisplayMemberPath="Nombre"
                              Style="{StaticResource FormComboBoxStyle}"/>
                </StackPanel>

                <StackPanel Grid.Row="0" Grid.Column="1" Margin="12,0,12,16">
                    <TextBlock Text="Estado" Style="{StaticResource FormLabelStyle}"/>
                    <ComboBox ItemsSource="{Binding Estados}"
                              SelectedItem="{Binding EstadoSeleccionado}"
                              DisplayMemberPath="Nombre"
                              Style="{StaticResource FormComboBoxStyle}"/>
                </StackPanel>

                <StackPanel Grid.Row="0" Grid.Column="2" Margin="12,0,0,16">
                    <TextBlock Text="Ubicación - Sede" Style="{StaticResource FormLabelStyle}"/>
                    <ComboBox ItemsSource="{Binding Sedes}"
                              SelectedItem="{Binding SedeSeleccionada}"
                              DisplayMemberPath="Nombre"
                              Style="{StaticResource FormComboBoxStyle}"/>
                </StackPanel>

                <!-- Fila 2 -->
                <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,0,12,16">
                    <TextBlock Text="Área" Style="{StaticResource FormLabelStyle}"/>
                    <ComboBox ItemsSource="{Binding Areas}"
                              SelectedItem="{Binding AreaSeleccionada}"
                              DisplayMemberPath="Nombre"
                              Style="{StaticResource FormComboBoxStyle}"
                              IsEnabled="{Binding SedeSeleccionada, Converter={StaticResource NotNullConverter}}"/>
                </StackPanel>

                <StackPanel Grid.Row="1" Grid.Column="1" Margin="12,0,12,16">
                    <TextBlock Text="Zona" Style="{StaticResource FormLabelStyle}"/>
                    <ComboBox ItemsSource="{Binding Zonas}"
                              SelectedItem="{Binding ZonaSeleccionada}"
                              DisplayMemberPath="Nombre"
                              Style="{StaticResource FormComboBoxStyle}"
                              IsEnabled="{Binding AreaSeleccionada, Converter={StaticResource NotNullConverter}}"/>
                </StackPanel>

                <StackPanel Grid.Row="1" Grid.Column="2" Margin="12,0,0,16">
                    <TextBlock Text="Empleado" Style="{StaticResource FormLabelStyle}"/>
                    <ComboBox ItemsSource="{Binding Empleados}"
                              SelectedItem="{Binding EmpleadoSeleccionado}"
                              DisplayMemberPath="NombreCompleto"
                              Style="{StaticResource FormComboBoxStyle}"/>
                </StackPanel>

                <!-- Fila 3 -->
                <StackPanel Grid.Row="2" Grid.Column="0" Margin="0,0,12,0">
                    <TextBlock Text="Fecha Desde" Style="{StaticResource FormLabelStyle}"/>
                    <DatePicker SelectedDate="{Binding FechaDesde}" Style="{StaticResource FormDatePickerStyle}"/>
                </StackPanel>

                <StackPanel Grid.Row="2" Grid.Column="1" Margin="12,0,12,0">
                    <TextBlock Text="Fecha Hasta" Style="{StaticResource FormLabelStyle}"/>
                    <DatePicker SelectedDate="{Binding FechaHasta}" Style="{StaticResource FormDatePickerStyle}"/>
                </StackPanel>

                <StackPanel Grid.Row="2" Grid.Column="2" Margin="12,0,0,0" Orientation="Horizontal" VerticalAlignment="Bottom">
                    <CheckBox Content="Incluir inactivos"
                              IsChecked="{Binding IncluirInactivos}"
                              VerticalAlignment="Center"
                              Margin="0,0,16,0"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Resultados - Tabla con anchos optimizados -->
        <Border Grid.Row="2"
                Margin="24,16,24,16"
                BorderThickness="1"
                BorderBrush="#E5E7EB"
                CornerRadius="8"
                Background="White"
                Padding="1">
            <DataGrid ItemsSource="{Binding Resultados}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      HeadersVisibility="Column"
                      GridLinesVisibility="Horizontal"
                      HorizontalGridLinesBrush="#EEF1F5"
                      AlternatingRowBackground="#FAFAFB"
                      RowBackground="White"
                      AlternationCount="2"
                      RowHeaderWidth="0"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      CanUserReorderColumns="True"
                      CanUserResizeRows="False"
                      CanUserSortColumns="True"
                      BorderThickness="0"
                      SelectionMode="Single"
                      SelectionUnit="FullRow"
                      ScrollViewer.CanContentScroll="True"
                      ScrollViewer.HorizontalScrollBarVisibility="Auto"
                      ScrollViewer.VerticalScrollBarVisibility="Auto"
                      EnableRowVirtualization="True"
                      EnableColumnVirtualization="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Num. Serie" 
                                        Binding="{Binding NumeroSerie}" 
                                        Width="110"
                                        ElementStyle="{StaticResource CellTextStyle}"/>
                    <DataGridTextColumn Header="Etiqueta" 
                                        Binding="{Binding EtiquetaInventario}" 
                                        Width="100"
                                        ElementStyle="{StaticResource CellTextStyle}"/>
                    <DataGridTextColumn Header="Marca" 
                                        Binding="{Binding Marca}" 
                                        Width="90"
                                        ElementStyle="{StaticResource CellTextStyle}"/>
                    <DataGridTextColumn Header="Modelo" 
                                        Binding="{Binding Modelo}" 
                                        Width="100"
                                        ElementStyle="{StaticResource CellTextStyle}"/>
                    <DataGridTextColumn Header="Tipo" 
                                        Binding="{Binding TipoEquipo}" 
                                        Width="110"
                                        ElementStyle="{StaticResource CellTextStyle}"/>
                    <DataGridTextColumn Header="Estado" 
                                        Binding="{Binding Estado}" 
                                        Width="100"
                                        ElementStyle="{StaticResource CellTextStyle}"/>
                    <DataGridTextColumn Header="Asignado a" 
                                        Binding="{Binding UsuarioAsignado}" 
                                        Width="140"
                                        ElementStyle="{StaticResource CellTextStyle}"/>
                    <DataGridTextColumn Header="Ubicación" 
                                        Binding="{Binding Ubicacion}" 
                                        Width="150"
                                        ElementStyle="{StaticResource CellTextStyle}"/>
                    <DataGridTextColumn Header="Adquisición" 
                                        Binding="{Binding FechaAdquisicion, StringFormat='{}{0:dd/MM/yyyy}'}" 
                                        Width="100"
                                        ElementStyle="{StaticResource CellTextStyle}"/>
                    <DataGridTemplateColumn Header="Activo" Width="80">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Activo, Converter={StaticResource BoolToStringConverter}}"
                                           HorizontalAlignment="Center"
                                           VerticalAlignment="Center"
                                           Style="{StaticResource CellTextStyle}"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <!-- Acciones - Botones con estilos consistentes -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,24,24">
            <Button Content="Limpiar Filtros" 
                    Command="{Binding LimpiarFiltrosCommand}" 
                    Margin="0,0,8,0"
                    Style="{StaticResource SecondaryButtonStyle}"/>

            <Button Content="Generar Reporte" 
                    Command="{Binding GenerarReporteCommand}" 
                    Margin="0,0,8,0"
                    Style="{StaticResource PrimaryButtonStyle}"/>

            <Button Content="Exportar a PDF" 
                    Command="{Binding ExportarPdfCommand}" 
                    Margin="0,0,8,0"
                    Style="{StaticResource ExportButtonStyle}"
                    Background="#8E44AD"/>

            <Button Content="Exportar a Excel" 
                    Command="{Binding ExportarExcelCommand}" 
                    Style="{StaticResource ExportButtonStyle}"
                    Background="#27AE60"/>
        </StackPanel>

        <!-- Overlay de carga -->
        <Grid Grid.Row="0" Grid.RowSpan="4"
              Background="#80000000"
              Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}">
            <Border Padding="20" 
                    BorderThickness="1" 
                    BorderBrush="#E1E5EB" 
                    CornerRadius="8" 
                    Background="White"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center">
                <StackPanel>
                    <TextBlock Text="Generando reporte..." 
                               FontSize="16" 
                               Margin="0,0,0,10"/>
                    <ProgressBar IsIndeterminate="True" 
                                 Height="4" 
                                 Width="220"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>