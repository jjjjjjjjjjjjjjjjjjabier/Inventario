<UserControl x:Class="InventarioComputo.UI.Views.ReportesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:InventarioComputo.UI.Views"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800"
             Loaded="UserControl_Loaded">

    <UserControl.Resources>
        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#0078D7"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="15,5"/>
            <Setter Property="MinWidth" Value="100"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}"
                                CornerRadius="20">
                            <ContentPresenter HorizontalAlignment="Center" 
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#0063B1"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#CCCCCC"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#EFEFEF"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="Padding" Value="15,5"/>
            <Setter Property="MinWidth" Value="100"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}"
                                CornerRadius="20">
                            <ContentPresenter HorizontalAlignment="Center" 
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#DDDDDD"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="GroupBox">
            <Setter Property="Margin" Value="0,0,0,15"/>
            <Setter Property="Padding" Value="15"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="Background" Value="White"/>
        </Style>

        <Style TargetType="TextBlock" x:Key="LabelStyle">
            <Setter Property="Margin" Value="0,0,0,5"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" 
                   Text="Reportes de Equipos"
                   FontSize="24" 
                   FontWeight="Bold"
                   Margin="0,0,0,20"
                   Foreground="#2C3E50"/>

        <!-- Filtros -->
        <GroupBox Grid.Row="1" 
                  Header="Filtros" 
                  Margin="0,0,0,15"
                  BorderThickness="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Primera fila -->
                <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,0,10,10">
                    <TextBlock Text="Tipo de Equipo:" Style="{StaticResource LabelStyle}"/>
                    <ComboBox ItemsSource="{Binding TiposEquipo}"
                              SelectedItem="{Binding TipoEquipoSeleccionado}"
                              DisplayMemberPath="Nombre"/>
                </StackPanel>

                <StackPanel Grid.Row="0" Grid.Column="1" Margin="10,0,10,10">
                    <TextBlock Text="Estado:" Style="{StaticResource LabelStyle}"/>
                    <ComboBox ItemsSource="{Binding Estados}"
                              SelectedItem="{Binding EstadoSeleccionado}"
                              DisplayMemberPath="Nombre"/>
                </StackPanel>

                <StackPanel Grid.Row="0" Grid.Column="2" Margin="10,0,0,10">
                    <TextBlock Text="Usuario:" Style="{StaticResource LabelStyle}"/>
                    <ComboBox ItemsSource="{Binding Usuarios}"
                              SelectedItem="{Binding UsuarioSeleccionado}"
                              DisplayMemberPath="NombreCompleto"/>
                </StackPanel>

                <!-- Segunda fila -->
                <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,0,10,10">
                    <TextBlock Text="Área:" Style="{StaticResource LabelStyle}"/>
                    <ComboBox ItemsSource="{Binding Areas}"
                              SelectedItem="{Binding AreaSeleccionada}"
                              DisplayMemberPath="Nombre"/>
                </StackPanel>

                <StackPanel Grid.Row="1" Grid.Column="1" Margin="10,0,10,10">
                    <TextBlock Text="Zona:" Style="{StaticResource LabelStyle}"/>
                    <ComboBox ItemsSource="{Binding Zonas}"
                              SelectedItem="{Binding ZonaSeleccionada}"
                              DisplayMemberPath="Nombre"/>
                </StackPanel>

                <!-- Tercera fila -->
                <StackPanel Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="3" Orientation="Horizontal">
                    <Button Content="Generar Reporte" 
                            Style="{StaticResource ActionButtonStyle}"
                            Command="{Binding GenerarReporteCommand}"
                            Margin="0,0,10,0"/>

                    <Button Content="Limpiar Filtros" 
                            Style="{StaticResource SecondaryButtonStyle}"
                            Command="{Binding LimpiarFiltrosCommand}"/>
                </StackPanel>
            </Grid>
        </GroupBox>

        <!-- Resultados -->
        <DataGrid Grid.Row="2" 
                  ItemsSource="{Binding Equipos}"
                  AutoGenerateColumns="False"
                  Margin="0,10,0,0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Etiqueta" Binding="{Binding EtiquetaInventario}" Width="120"/>
                <DataGridTextColumn Header="Número Serie" Binding="{Binding NumeroSerie}" Width="120"/>
                <DataGridTextColumn Header="Marca" Binding="{Binding Marca}" Width="100"/>
                <DataGridTextColumn Header="Modelo" Binding="{Binding Modelo}" Width="100"/>
                <DataGridTextColumn Header="Tipo" Binding="{Binding TipoEquipo.Nombre}" Width="100"/>
                <DataGridTextColumn Header="Estado" Binding="{Binding Estado.Nombre}" Width="100"/>
                <DataGridTextColumn Header="Ubicación" Binding="{Binding Zona.Nombre}" Width="120"/>
                <DataGridTextColumn Header="Usuario" Binding="{Binding Usuario.NombreCompleto}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Botones exportar -->
        <StackPanel Grid.Row="3" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right"
                    Margin="0,15,0,0">
            <Button Content="Exportar a Excel" 
                    Style="{StaticResource ActionButtonStyle}"
                    Command="{Binding ExportarExcelCommand}"
                    Margin="0,0,10,0"/>

            <Button Content="Exportar a PDF" 
                    Style="{StaticResource ActionButtonStyle}"
                    Command="{Binding ExportarPdfCommand}"/>
        </StackPanel>

        <!-- Loading indicator -->
        <Grid Grid.Row="0" Grid.RowSpan="4" Background="#80FFFFFF"
              Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Text="Cargando..." 
                           FontSize="16"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,10"/>
                <ProgressBar IsIndeterminate="True" 
                             Width="200" 
                             Height="10"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>