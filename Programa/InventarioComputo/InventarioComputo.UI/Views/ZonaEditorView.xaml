<Window x:Class="InventarioComputo.UI.Views.ZonaEditorView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding Titulo}" WindowStartupLocation="CenterOwner" SizeToContent="WidthAndHeight">
    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="{Binding Titulo}" FontSize="18" FontWeight="Bold" Margin="0,0,0,12"/>

        <StackPanel Grid.Row="1">
            <TextBlock Text="Nombre:" Style="{StaticResource FormLabelStyle}"/>
            <TextBox Text="{Binding Nombre, UpdateSourceTrigger=PropertyChanged}" Padding="6,4" Margin="0,0,0,10"/>
            <CheckBox Content="Activo" IsChecked="{Binding Activo}" Style="{StaticResource CheckBoxStyle}"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,14,0,0">
            <Button Content="Guardar" Command="{Binding GuardarCommand}" Style="{StaticResource PrimaryButton}" Margin="0,0,8,0"/>
            <Button Content="Cancelar" Command="{Binding CloseCommand}" Style="{StaticResource SecondaryButton}"/>
        </StackPanel>
    </Grid>
</Window>